# Project Rules

- Do not write tests. Do not create `.spec.ts` or `.test.ts` files.
- Swagger API docs available at http://localhost:3080/api
- I keep dev server running at 3080 port with script start:dev. Agent can run its own dev server at port 3082.
